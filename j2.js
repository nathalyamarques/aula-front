// aula Js

function oi()
{
    console.log(valor.value);
}

function mais(){
    console.log(valor.value.toUpperCase());
}

function primeira (){
    var texto = valor.value.toLowerCase();
    texto = texto[0].toUpperCase() + texto.slice(1);
    console.log(texto);
}

function contagem() {
    var numero = valor.value;

    while (numero >=0){
        var contagem = numero;
        console.log(contagem);
        numero = numero - 1;
    }
}